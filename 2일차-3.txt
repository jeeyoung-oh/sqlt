   idx.sql 파일 c:\sqlt 폴더에 복사 
 sqlplus scott/tiger
 SQL> conn scott/tiger
 
 SQL> @c:\sqlt\idx
   --> 테이블명 emp 입력

 SQL> create index emp_ename_i on emp(ename) ; 
 SQL> @c:\sqlt\idx
           emp
        
 -- 어떤조건사용시 인덱스 이용되는지 확인
 SQL> alter session set optimizer_mode=rule; 
SQL>  set autot on exp

SQL> select ename,sal from emp where empno =7788 ; 
        select ename,sal from emp where empno > 7788; 
        select ename,sal from emp  where empno <> 7788;
    
       select ename,sal from emp  where empno in ( 7788, 7902) ; 
      select ename, sal from emp where empno between 7900 and 7920 ; 

      select ename,sal from emp  where empno NOT in ( 7788, 7902) ; 
     select ename, sal from emp where empno NOT between 7900 and 7920 ; 
      select ename,sal from emp  where NOT ( empno > 7900) ; 

 SQL> select  ename, sal from emp 
          where ename > 'TOM' ; 

 Q>   emp테이블 이름 첫번째글자가  A문자인 사원만 이름,급여조회. 

  1) select ename,sal from emp 
      where SUBSTR( ename,1,1) ='A'; 
 
 2) select ename, sal from emp 
      where ename LIKE 'A%' ; 


 비교> select ename,sal from emp 
          where  ename like '_A%' ; 
                                   '%A%'






